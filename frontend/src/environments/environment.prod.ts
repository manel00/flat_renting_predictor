// Production environment configuration for Render deployment
export const environment = {
    production: true,
    // Update this URL after deploying the backend to Render
    // It will be something like: https://housing-backend-XXXX.onrender.com/api
    apiUrl: 'https://housing-backend.onrender.com/api'
};
